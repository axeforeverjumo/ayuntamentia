/* Temporal test fixture intentionally kept out of lint scope.
   Original experimental CommonJS test left as inert placeholder to avoid
   unrelated ESLint failures while preserving the path for local references. */
module.exports = {};
